DLLs needed if Microsoft compiler is used

MWetter@lbl.gov                   2009-08-11
############################################

This directory contains dlls that
are required if the Microsoft compiler
is used.